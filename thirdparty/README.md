# thirdparty/

第三方公开开源代码的暂存区。Phase 1.1 用 —— 拷入 CommonModule 之前的中转。

放进来的子目录都应该带原始 LICENSE，且目录名 = upstream 项目名 + 版本号
（例如 `cJSON-1.7.18/`、`xxtea-cpp-cb53de7/`）。

正式入仓的文件最终位于 `Y:/develop/github/backmir/CommonModule/`，并在文件
顶部注释里写明来源 URL 与 commit/版本——这个目录只是工作区，不是构建产物
依赖路径。
